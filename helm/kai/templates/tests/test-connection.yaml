apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kai.fullname" . }}-test-connection"
  labels:
    {{- include "kai.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kai.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
